services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: biblio_ia_app
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/instance
    restart: always